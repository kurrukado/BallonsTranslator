# To install pytorch cuda (gpu) version, please look https://pytorch.org/

# GUI Framework
PyQt6-Qt6>=6.6.2 ; python_version > "3.8"
PyQt6>=6.6.1 ; python_version > "3.8"
PyQt5-Qt5>=5.15.2 ; python_version <= "3.8"
PyQt5>=5.15.10 ; python_version <= "3.8"
qtpy

# Core Deep Learning & Computer Vision
torch
torchvision
transformers>=4.38.0
opencv-python>=4.8.1.78; sys_platform == 'win32' or sys_platform == 'linux'
numpy
einops
shapely
pyclipper
pyyaml

# Local OCR Engines (Local AI & Windows Native)
rapidocr-onnxruntime
onnxruntime
manga-ocr
fugashi
unidic_lite
jaconv
winrt-Windows.Media.Ocr; sys_platform == 'win32'
winrt-Windows.Globalization; sys_platform == 'win32'

# LLM Translation & API Client
openai>=1.50.0
httpx
requests
urllib3
beautifulsoup4

# Text Processing & Utilities
tqdm
termcolor
colorama
natsort
opencc-python-reimplemented
ordered-set
piexif
pynput
python-docx
docx2txt
json5
pillow-jxl-plugin>=1.3.4
betterproto
psutil
nest-asyncio
py7zr
multivolumefile

# Chinese Segmentation (optional, legacy Python only)
spacy-pkuseg; python_version < "3.12"

# Windows Platform Specifics
pywin32; sys_platform == 'win32'
brotlicffi; sys_platform == 'win32'
msl-loadlib; sys_platform == 'win32'

# macOS Platform Specifics
mecab-python3; sys_platform == 'darwin'
pyobjc-core; sys_platform == 'darwin'
pyobjc-framework-cocoa; sys_platform == 'darwin'
pyobjc-framework-coreml; sys_platform == 'darwin'
pyobjc-framework-quartz; sys_platform == 'darwin'
pyobjc-framework-vision; sys_platform == 'darwin'

# Optional Modules (uncomment if using YOLO detector or Flux.2 inpainting)
# ultralytics>=8.4.14
# diffusers>=0.30.0
# gguf>=0.10.0
# hf_transfer